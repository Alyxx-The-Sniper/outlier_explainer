# OutlierExplainer

A simple Python tool to detect and explain outliers in datasets using Isolation Forest, One-Class SVM, or Local Outlier Factor — with LIME-based explanations and 2D/3D visualizations via PCA or UMAP.

## 🔧 Features

- Outlier detection via:
  - Isolation Forest
  - One-Class SVM
  - Local Outlier Factor
- LIME-based instance explanations
- 2D and 3D visualization using PCA or UMAP
- Easy to integrate with any `pandas` DataFrame

## 📦 Installation

Install directly from GitHub:

```bash
pip install git+https://github.com/Alyxx-The-Sniper/outlier_explainer.git